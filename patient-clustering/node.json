{
  "version": "1.0",
  "nodeType": "processing",
  "name": "Patient Measurement Clustering",
  "shortDescription": "Cluster patients by clinical measurement scores using KMeans",
  "description": "Takes an OMOP-format measurement table (Parquet) and clusters patients based on their clinical scores (e.g. HAM-D, PSQI, CDR). Pivots the data so each patient becomes one row, runs KMeans clustering, and outputs the original table with a cluster label column added. Useful for identifying patient subgroups for further analysis in Superset.",
  "githubUrl": "https://github.com/<your-github-username>/custom-clustering-original",
  "githubRef": "main",
  "lucideIcon": "Users",
  "color": "#10b981",
  "hasInput": true,
  "inputDescription": "OMOP measurement table as Parquet with columns: person_id, measurement_source_value, value_as_number",
  "config": {
    "name": {
      "type": "string",
      "label": "Name",
      "description": "Unique identifier for this node in the pipeline",
      "required": true,
      "unique": true,
      "placeholder": "e.g. cluster_patients",
      "validation": {
        "patternKey": "snake_case"
      }
    },
    "n_clusters": {
      "type": "select",
      "label": "Number of Clusters",
      "description": "How many patient groups to create. 3 is a good default for most clinical datasets.",
      "required": false,
      "unique": false,
      "options": [
        { "value": "2", "label": "2 clusters" },
        { "value": "3", "label": "3 clusters (default)" },
        { "value": "4", "label": "4 clusters" },
        { "value": "5", "label": "5 clusters" }
      ]
    }
  },
  "hasOutput": true,
  "outputDescription": "Original measurement table with an added 'cluster' column (integer, 0-indexed) per patient",
  "outputFiles": [
    {
      "name": "result",
      "format": "parquet",
      "path": "{{artifacts}}/{{pipeline.name}}/{{execution.timestamp}}/{{node.name}}/result.parquet"
    }
  ],
  "implementation": {
    "type": "container",
    "entrypoint": "python transform.py",
    "dockerfilePath": "Dockerfile",
    "dockerContext": "measurement-clustering"
  }
}
